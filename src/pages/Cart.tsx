import React from 'react';
import {ScrollView, Text} from 'react-native';

const Cart = (): JSX.Element => {
  return (
    <ScrollView>
      <Text>Cart</Text>
    </ScrollView>
  );
};

export default Cart;

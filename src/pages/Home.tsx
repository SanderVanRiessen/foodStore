import React from 'react';
import {ScrollView, Text} from 'react-native';

const Home = (): JSX.Element => {
  return (
    <ScrollView>
      <Text>Home</Text>
    </ScrollView>
  );
};

export default Home;

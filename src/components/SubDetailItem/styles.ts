import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  subDetailText: {
    fontSize: 13,
    color: '#333',
    flexGrow: 1,
    flexBasis: 0,
  },
  subDetailItem: {
    display: 'flex',
    flexDirection: 'row',
  },
});

export default styles;

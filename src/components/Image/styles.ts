import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 150,
    height: 150,
    margin: 20,
  },
  image: {
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
    borderRadius: 10,
  },
});

export default styles;
